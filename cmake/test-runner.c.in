#include <stdio.h>

void main(int argc, char *argv[]) {
  char *working_dir = "@PROJECT_BINARY_DIR@";
  char *command = "@MOAI_BINARY_PATH@ @TEST_PROJECT_TARGET@";
  printf("Executing: %s\n", command);
  chdir(working_dir);
  system(command);
}
